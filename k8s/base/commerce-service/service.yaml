apiVersion: v1
kind: Service
metadata:
  name: commerce-service
  labels:
    app.kubernetes.io/name: commerce-service
    app.kubernetes.io/part-of: shoppinglive
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: commerce-service
  ports:
    - name: http
      port: 8080
      targetPort: http
