apiVersion: v1
kind: Service
metadata:
  name: live-service
  labels:
    app.kubernetes.io/name: live-service
    app.kubernetes.io/part-of: shoppinglive
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: live-service
  ports:
    - name: http
      port: 8080
      targetPort: http
