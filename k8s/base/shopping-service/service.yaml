apiVersion: v1
kind: Service
metadata:
  name: shopping-service
  labels:
    app.kubernetes.io/name: shopping-service
    app.kubernetes.io/part-of: shoppinglive
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: shopping-service
  ports:
    - name: http
      port: 8080
      targetPort: http
